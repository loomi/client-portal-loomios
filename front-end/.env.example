# Base URL of the back-end API (must include the /api prefix)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Chave da API da Anthropic — usada apenas no server (route /api/triagem). Nunca commitar.
ANTHROPIC_API_KEY=
